<h1><%= @task.title %></h1>
<p><%= @task.details %></p>
<p><%= link_to 'Edit task', edit_task_path %></p>
<p><%= link_to "Delete task", task_path(@task), data: {turbo_method: :delete, turbo_confirm: "Do you really want to delete #{@task.title} ?"} %></p>
<p><%= link_to 'Back to the list', tasks_path %></p>
